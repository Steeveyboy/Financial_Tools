# Core dependencies (required)
python-dotenv~=1.0
sqlalchemy~=2.0
psycopg2-binary~=2.9     # only needed for PostgreSQL

# Progress bar
tqdm~=4.66

# RSS extraction
feedparser~=6.0
beautifulsoup4~=4.14
lxml~=6.0                # HTML parser backend for BeautifulSoup

# HuggingFace dataset (FNSPID)
datasets~=2.20

# Sentiment analysis — FinBERT (ProsusAI/finbert), run locally.
# Weights download on first use and cache under ~/.cache/huggingface.
# For a CPU-only install, a much smaller torch wheel is available from
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
torch~=2.0
transformers~=4.40

# Entity extraction — uncomment one approach (see transformers/entity.py)
# Option A: dictionary lookup — no extra deps
#
# Option B: spaCy NER
# spacy~=3.7
