# Dependencies for the corporate_db module
# ==========================================
# Install with:  pip install -r corporate_db/requirements.txt

sqlalchemy>=2.0
alembic>=1.13
psycopg2-binary>=2.9     # PostgreSQL driver (not needed for SQLite-only use)
python-dotenv>=1.0       # Load .env files for DATABASE_URL etc.
